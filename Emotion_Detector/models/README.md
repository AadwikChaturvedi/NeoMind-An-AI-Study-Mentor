This folder is where `train.py` saves `trained_model.pth`.

It's empty right now on purpose — the original checkpoint from the
notebook was never committed anywhere and no longer exists. Run
`python train.py --data-dir <dataset folder>` from `Emotion_Detector/`
to create it.
